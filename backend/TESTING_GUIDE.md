# Руководство по тестированию системы аукционов

## Предварительные требования

1. **MongoDB** должен быть запущен
2. **Redis** должен быть запущен
3. **Переменные окружения** настроены в `.env`

## Способ 1: Автоматические тесты (Vitest)

### Запуск всех тестов

```bash
cd backend
pnpm test
```

### Запуск конкретного теста

```bash
pnpm test auctionFullCycle
pnpm test bidService
pnpm test settlementService
```

### Запуск с покрытием

```bash
pnpm test:cov
```

## Способ 2: Ручное тестирование (Manual Test Script)

### Запуск скрипта ручного тестирования

```bash
cd backend
pnpm tsx src/__tests__/manualAuctionTest.ts
```

Этот скрипт автоматически:
1. ✅ Создает тестовую коллекцию
2. ✅ Создает 5 тестовых пользователей с балансом 1000
3. ✅ Создает аукцион
4. ✅ Размещает ставки от всех пользователей
5. ✅ Проверяет замороженные балансы
6. ✅ Закрывает раунд
7. ✅ Проверяет settlement (победители/проигравшие)
8. ✅ Проверяет переход к следующему раунду
9. ✅ Завершает аукцион
10. ✅ Очищает тестовые данные

## Способ 3: Тестирование через API и WebSocket

### 1. Запустить сервер

```bash
cd backend
pnpm start:dev
```

### 2. Создать коллекцию

```bash
curl -X POST http://localhost:3000/collections \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer YOUR_ADMIN_TOKEN" \
  -d '{
    "title": "Test Collection",
    "total_amount": 10,
    "emoji": "🎁"
  }'
```

### 3. Создать аукцион

```bash
curl -X POST http://localhost:3000/auctions \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer YOUR_ADMIN_TOKEN" \
  -d '{
    "collection_id": "COLLECTION_ID",
    "round_duration": 60,
    "gifts_per_round": 3
  }'
```

### 4. Подключиться к WebSocket

```javascript
const ws = new WebSocket('ws://localhost:3000/ws/auction?token=YOUR_JWT_TOKEN');

ws.onopen = () => {
  console.log('Connected');
  
  // Subscribe to auction
  ws.send(JSON.stringify({
    type: 'subscribe',
    data: { auctionId: 'AUCTION_ID' }
  }));
  
  // Place bid
  ws.send(JSON.stringify({
    type: 'bid',
    data: {
      auctionId: 'AUCTION_ID',
      amount: 100
    }
  }));
};

ws.onmessage = (event) => {
  const message = JSON.parse(event.data);
  console.log('Received:', message);
};
```

## Проверочный список тестирования

### ✅ Базовый функционал

- [ ] Создание аукциона
- [ ] Аукцион автоматически запускается
- [ ] Первый раунд создается автоматически
- [ ] Redis state инициализируется

### ✅ Размещение ставок

- [ ] Успешное размещение ставки
- [ ] Ставка замораживает баланс
- [ ] Невозможность разместить вторую ставку
- [ ] Невозможность разместить ставку при недостаточном балансе
- [ ] Невозможность разместить ставку когда раунд settling

### ✅ Закрытие раунда

- [ ] Раунд закрывается по таймеру
- [ ] Флаг settling устанавливается
- [ ] Новые ставки блокируются

### ✅ Settlement

- [ ] Победители определяются корректно (топ-N)
- [ ] При равенстве побеждает более ранняя ставка
- [ ] Победителям списывается баланс
- [ ] Проигравшим возвращается баланс
- [ ] Settlement идемпотентен (не выполняется дважды)

### ✅ Переход между раундами

- [ ] Следующий раунд создается автоматически
- [ ] Непроданные подарки переносятся в следующий раунд
- [ ] Redis state обновляется
- [ ] Новый таймер создается

### ✅ Завершение аукциона

- [ ] Аукцион завершается при отсутствии подарков
- [ ] Все замороженные средства возвращаются
- [ ] Redis ключи очищаются
- [ ] Статус меняется на `finished`

### ✅ Recovery

- [ ] При рестарте активные аукционы восстанавливаются
- [ ] Redis state восстанавливается из MongoDB
- [ ] Таймеры пересоздаются
- [ ] Замороженные балансы восстанавливаются

### ✅ WebSocket события

- [ ] `bid_placed` отправляется при новой ставке
- [ ] `round_started` отправляется при начале раунда
- [ ] `round_closed` отправляется при закрытии раунда
- [ ] `round_settled` отправляется после settlement
- [ ] `auction_finished` отправляется при завершении

## Тестовые сценарии

### Сценарий 1: Полный цикл с победителями

1. Создать аукцион (10 подарков, 3 на раунд)
2. Разместить 5 ставок: 500, 400, 300, 200, 100
3. Дождаться закрытия раунда
4. Проверить: топ-3 (500, 400, 300) - победители, остальные - проигравшие
5. Проверить балансы: победители - списано, проигравшие - возвращено

### Сценарий 2: Tie-breaking

1. Создать аукцион (2 подарка на раунд)
2. Разместить 3 ставки с одинаковой суммой: 300, 300, 300
3. Дождаться settlement
4. Проверить: победители - первые 2 по времени

### Сценарий 3: Недостаточный баланс

1. Создать аукцион
2. Попытаться разместить ставку больше баланса
3. Проверить: ставка отклонена, пользователь не добавлен в SET

### Сценарий 4: Дубликат ставки

1. Создать аукцион
2. Разместить ставку
3. Попытаться разместить вторую ставку
4. Проверить: вторая ставка отклонена

### Сценарий 5: Завершение при отсутствии подарков

1. Создать аукцион (10 подарков, 10 на раунд)
2. Разместить ставки
3. Дождаться settlement первого раунда
4. Проверить: аукцион завершен (нет подарков для следующего раунда)

## Ожидаемые результаты

### После размещения ставки:

- ✅ Пользователь добавлен в `auction:{id}:users`
- ✅ Ставка добавлена в `auction:{id}:round:{n}:bids`
- ✅ Заморожен баланс: `user:{userId}:frozen:{auctionId}`
- ✅ Доступный баланс уменьшен

### После settlement:

- ✅ Победители: баланс списан, frozen удален
- ✅ Проигравшие: баланс возвращен, frozen удален
- ✅ Установлен флаг `auction:{id}:round:{n}:settled`
- ✅ Ставки удалены из Redis

### После завершения аукциона:

- ✅ Статус в MongoDB = `finished`
- ✅ Все Redis ключи `auction:{id}:*` удалены
- ✅ Все frozen балансы возвращены

## Отладка

### Проверка Redis состояния

```bash
redis-cli
> KEYS auction:*
> HGETALL auction:{id}:state
> SMEMBERS auction:{id}:users
> ZRANGE auction:{id}:round:1:bids 0 -1 WITHSCORES
> GET auction:{id}:round:1:settled
```

### Проверка MongoDB

```javascript
// В MongoDB shell
db.auctions.find({ status: "active" })
db.rounds.find({ auction_id: ObjectId("...") })
db.ownerships.find({})
```

### Логи

Все события логируются через `pino`. Проверьте логи для:
- Размещения ставок
- Закрытия раундов
- Settlement
- Переходов между раундами

## Известные проблемы

Если тесты пропускаются:
- Проверьте, что Redis запущен: `redis-cli ping`
- Проверьте, что MongoDB запущен
- Проверьте переменные окружения в `.env`

Если settlement не выполняется:
- Проверьте, что RoundTimerService запущен
- Проверьте логи на наличие ошибок
- Убедитесь, что раунд действительно закрыт
